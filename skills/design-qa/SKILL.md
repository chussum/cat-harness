---
name: design-qa
description: 디자인 QA에서 "Figma랑 다르다/간격 다르다/픽셀 안 맞는다" 지적을 받았을 때 사용. Figma 캡쳐와 구현 렌더를 동일 크기로 겹쳐(overlay) 육안 대조하고, 잉크(ink) 픽셀 측정으로 오프셋을 수치화해 일치할 때까지 수정 루프를 돈다. 트리거: "디자인 QA", "피그마랑 달라", "간격 이슈", "겹쳐서 비교", "픽셀 맞춰줘".
---

# Design Pixel QA — Figma 겹침 대조 루프

Figma 디자인과 구현 화면을 픽셀 단위로 맞추는 검증·수정 루프. 실제 QA 세션에서 수십 번의 시행착오로 확립된 절차다. **아래 철칙을 어기면 같은 실수를 반복하게 된다.**

## 철칙 (전부 실제 사고에서 나온 규칙)

1. **반드시 겹쳐서 육안으로 판정한다.** 수치 측정은 원인 추적용이고, 최종 판정은 항상 "두 이미지를 포갠 화면"을 눈으로 본다. 수치가 맞는데 눈에 다르면 측정이 틀린 것이다.
2. **두 캡쳐는 반드시 같은 픽셀 크기여야 한다.** Figma export가 375px면 구현 캡쳐도 375px. 뷰포트가 넓으면 `width:100%` 컴포넌트가 늘어나 캡쳐가 900px가 되고, 겹침이 전부 어긋나 보인다(실제 사고). 캡쳐 후 `sips`로 크기부터 대조하라.
3. **목데이터(사진·상품명·썸네일)는 회색으로 마스킹하고 비교한다.** 콘텐츠가 다르면 픽셀 diff가 전부 "다름"으로 나와 레이아웃 차이를 가린다. 겹침의 목적은 **글자·버튼의 위치/크기/간격**이다.
4. **가로만 재지 말고 세로도 잰다.** 세로 4px 오프셋을 가로 측정만 반복하며 못 찾는 사고가 있었다. 모든 요소를 가로 잉크 span + 세로 잉크 rows 둘 다 측정한다.
5. **비교하는 두 요소는 같은 색 판정식(threshold)으로 잰다.** 할인율(핑크/빨강)과 금액(검정)을 다른 판정식으로 재면 ±1px 가짜 오프셋이 나온다. 이걸 "보정"하면 멀쩡한 밑선을 망가뜨린다(실제 사고).
6. **크롬은 글자를 정수 픽셀에 스냅한다(DPR 1).** `position/top/margin` 0.5px 이동은 렌더에 반영되지 않는다(실측 확인). 위치 보정은 1px 단위, **폭 보정만 letter-spacing 소수점(0.1~0.15px)이 유효**하다.
7. **개별 텍스트에 세로 오프셋을 넣지 마라.** 같은 행의 두 텍스트(예: 할인율+금액)는 브라우저가 밑선을 자동 정렬한다. 한쪽에만 `top: 1px`을 넣으면 구현 화면 자체가 어긋난다. 행 전체(부모 row)를 함께 이동시켜라.
8. **±1 경계 안티앨리어싱 픽셀은 쫓지 마라.** 같은 CSS에서도 측정마다 301↔302로 흔들리는 픽셀이 있다. 이걸 맞추려 값을 바꾸면 다른 곳이 깨진다. 흔들림 이력을 기록하고 육안 겹침으로 판정한다.
9. **수정 1회 = 재캡쳐 + 재측정 + 겹침 1회.** 수정 후 검증 없이 다음 수정으로 넘어가지 않는다. 일치할 때까지 루프.
10. **Figma 수치(metadata)보다 잉크 실측이 우선한다.** Figma 노드 gap이 4px라 해도 실제 export 픽셀이 다르면 픽셀을 따른다. 단, 색상은 디자이너 확인 없이 바꾸지 마라(Figma가 옛 색일 수 있음 — 실제 사례: 할인율 핑크는 구버전, 현행 빨강 유지).
11. **측정 범위를 미리 가르지 마라.** "아이콘은 x≤256, 텍스트는 x≥256" 식으로 경계를 가정하고 재면 두 측정값이 경계에 인위적으로 딱 붙어 "일치"로 오판한다(실제 사고 — 아이콘·텍스트 간격 1px 차이를 이렇게 놓쳤다). 간격은 **두 요소를 모두 포함하는 전체 영역에서 hruns(컬럼 런)** 를 뽑아 런 사이 공백으로 계산한다.
12. **명목 스펙값을 기계적으로 적용하지 마라 — DS 에셋의 잉크 폭부터 재라.** Figma gap이 2px여도 DS 아이콘 잉크가 Figma 아이콘보다 1px 좁게 래스터되면 시각 간격은 1px 벌어진다(실제 사고: 벨 아이콘). 그리고 **정렬 방향에 따라 무엇이 움직이는지 계산하라** — 우측 정렬 pill에서 gap 변경은 아이콘만 움직이고(텍스트는 우측에 고정), 우측 패딩 변경이 아이콘+텍스트 그룹 전체를 움직인다. "어느 잉크를 몇 px 옮길지" 계산한 뒤에 CSS를 바꾼다.
13. **한 곳을 지적받아도 그 컴포넌트 전체를 전수 대조하라.** "간격 다르다"를 고치면 "이번엔 세로 위치가…"가 이어진다(실제 흐름 — 같은 배너에서 버튼 간격→그룹 가로위치→썸네일 텍스트 세로가 연달아 나옴). 지적이 오면 **가로·세로·간격·요소간 거리 전부**를 한 번에 표로 대조하고 한꺼번에 고친다(아래 "전수 대조" 절).
14. **글리프 전진폭(advance) 차이는 자간 스냅 실험으로 흡수한다.** 브라우저와 Figma는 글자별 전진폭이 1px씩 달라 어떤 배치로도 모든 글자를 못 맞추는 지점이 생긴다. 소수점 자간은 누적 반올림 위치를 바꾸므로 **0 / 0.15 / 0.2px 등 후보를 각각 실측해 일치 글자 수가 최대인 값**을 고른다(실제: 0→3/5자, 0.15→3/5자, 0.2→4/5자 일치). 남는 1px은 렌더러 차이로 주석에 기록하고 종료한다.

## 준비물

- **구현 렌더**: 검증 대상 컴포넌트를 격리해 띄운 화면 — 컴포넌트 뷰어, 전용 라우트, 또는 최소 재현 페이지 등 프로젝트에 맞는 무엇이든. 전체 앱 화면보다 **컴포넌트 격리 렌더가 픽셀 대조에 정확하다.**
- **Figma**: Figma MCP (`get_screenshot`으로 TO-BE export, `get_metadata`로 노드 구조/치수, `get_design_context`로 컴포넌트 매핑 — Code Connect로 디자인 시스템(DS, 공용 컴포넌트 모음) 컴포넌트가 지정된 경우 그 스펙이 정답).
- **캡쳐/측정**: Playwright MCP + 스크래치패드에 `python3 -m http.server 7020` (측정용 canvas HTML은 file:// 차단이라 HTTP로 서빙).

## 워크플로우

### 1. 캡쳐 (둘 다, 같은 크기)

```
# Figma TO-BE: get_screenshot(fileKey, nodeId) → curl로 저장
# 구현 AS-IS: 뷰포트를 Figma 프레임 폭(보통 375)으로 resize 후 element 캡쳐
browser_resize(375, 700)
browser_navigate("<컴포넌트를 격리 렌더한 URL>")   # 컴포넌트 뷰어 iframe / 전용 라우트 / 최소 재현 페이지
browser_evaluate: 캡쳐 대상 밖의 여백·배경 제거 CSS 주입 (컨테이너 margin/padding 0, 배경 흰색)
  'html,body{margin:0!important;padding:0!important;background:#fff!important;}
   <대상 컨테이너 셀렉터>{padding:0!important;margin:0!important;}'
browser_take_screenshot(target="<대상 컴포넌트 루트 셀렉터>", scale="device")
# 저장 후 반드시: sips로 두 PNG 크기 동일 확인
```

주의: Playwright 스크린샷은 **repo 루트**에 떨어진다 → 스크래치패드로 옮기고 `.playwright-mcp/` 삭제(깃 오염 방지).

**Figma 캡쳐 함정 (실제 발생):**
- `get_screenshot`이 **1×1 png**를 반환하면 export가 깨진 것(컴포넌트 마스터 등) — 같은 디자인의 **다른 인스턴스 노드**로 다시 찍는다.
- 반환 크기가 노드 폭보다 작으면(예: 323 노드가 73px) **부모 프레임에 클립**된 인스턴스다 — 부모 안에 완전히 들어있는 인스턴스를 찾아 찍는다.
- 겹침을 1:1로 하려면 **구현 렌더 폭이 Figma 노드 폭과 같아지는 뷰포트**를 고른다(반응형 카드면 노드 폭이 나오는 기기 폭으로 resize — 예: Figma 343 노드 ↔ 375 뷰포트의 343 카드, Figma 323 노드 ↔ 428 뷰포트의 323 카드).

### 2. 측정 (잉크 스캔 — references/measure-template.html)

canvas로 두 PNG를 읽어 **잉크 픽셀**(배경이 아닌 픽셀)의 가로 span/세로 rows를 요소별로 스캔한다. 색 판정식:

```js
const dark = (R,G,B)=>R<110&&G<110&&B<110;          // 검정 텍스트
const warm = (R,G,B)=>R>180&&G<130;                  // 빨강/핑크 (B 조건 넣으면 핑크 누락 — 실제 사고)
const blue = (R,G,B)=>B>150&&R<150&&B-R>40;          // 파랑 아이콘/텍스트
const white= (R,G,B)=>R>235&&G>235&&B>235;           // 흰 pill 배경
```

요소마다 `{ours: [L,R]/[top,bottom], figma: [...]}"` 표를 만들어 오프셋을 확정한다.

**잉크가 못 재는 곳은 DOM + Figma 좌표로 잰다.** 배경이 사진(노이즈)이라 판정식이 안 서는 텍스트(썸네일 위 오버레이 등)는:
1. `get_metadata`로 Figma 노드 트리의 **절대 좌표·박스 높이**를 표로 만든다.
2. 구현에서 `getBoundingClientRect()`(컴포넌트 루트 기준 상대좌표) + `getComputedStyle().lineHeight`를 전 요소 일괄 수집한다.
3. 두 표를 붙여 x/y/h를 1:1 대조한다 — 이 방법이 잉크보다 빠르고, 문구가 목데이터라 달라도 흔들리지 않는다. 최종 확인만 겹침 육안으로.

### 2.5 전수 대조 (부분 지적 → 전체 검증)

디자이너가 한 군데를 지적하면 그 **컴포넌트의 모든 요소**를 위 DOM+Figma 표로 한 번에 대조한다(가로 x·세로 y·박스 h·요소간 거리). 실제 사례: "버튼 간격" 지적 → 전수 대조에서 썸네일 텍스트 세로 4px/2px 처짐이 추가로 발견됨. 표에서 어긋난 것만 모아 **일괄 수정 → 일괄 재검증**이 지적 1건당 왕복 1회보다 훨씬 빠르고 신뢰를 준다.

### 3. 원인 매핑 (자주 나오는 실제 원인)

| 증상 | 실제 원인 | 수정 |
|---|---|---|
| 아래 요소들이 전부 N px 위로 밀림 | DS Text 컴포넌트 기본 line-height < Figma 텍스트박스 높이 | 해당 텍스트에 `line-height: <Figma값>px` |
| 행 안의 그룹이 1~2px 밀림 | flex `align-items:center`의 높이 재분배 | `flex-start` + 명시적 gap으로 전환 |
| 텍스트 폭이 1px 좁음/넓음 | 브라우저 vs Figma 글리프 advance 차이 | `letter-spacing: ±0.1~0.15px` |
| 버튼이 큼/작음 | 커스텀 pill이 DS 표준 버튼 크기와 다름 | `box-sizing:border-box`+`height` 고정+패딩 계산 |
| 버튼 내용 쏠림 | Figma는 좌측 편향인데 구현은 가운데(또는 반대) | 비대칭 패딩으로 잉크 x좌표 맞춤 |
| 이미지가 텍스트 위/아래 반대 | DS BottomSheet(바텀시트) 컴포넌트의 `imagePosition` 류 속성 | `'top'`↔`'bottom'` |
| **하단 고정 블록의 텍스트가 아래로 처짐** | absolute bottom + padding-bottom 배치에서 DS 기본 line-height < Figma 박스 → 짧아진 만큼 아래로 (실제: 스토어명 4px·날짜 2px) | 각 텍스트 `line-height: <Figma박스>px` |
| **하단 고정 스택에서 위 요소일수록 더 밀림** | 텍스트 박스 부족분이 위로 누적 (실제: 타이틀 3px+날짜 3px → 배지 6px) | 스택 내 모든 텍스트 line-height 고정 |
| 아이콘-텍스트 간격만 1px 넓음 | DS 아이콘 잉크가 Figma 아이콘보다 좁게 래스터 | gap을 잉크 기준으로 -1 (철칙 12 — 정렬 방향 계산 필수) |
| 아이콘+텍스트 그룹이 통째로 밀림 | 우측 정렬 컨테이너에서 우측 패딩 차이 | 우측 패딩으로 그룹 이동 (gap은 그룹을 못 움직임) |
| 문자열 중간 글자부터 1px 어긋남 | 글리프 전진폭 렌더 차이 (균일 시프트 아님 = 위치 문제 아님) | 자간 0.15/0.2px 후보 실측 비교 (철칙 14) |
| 특정 텍스트만 가로 1~2px 어긋남 | Figma에서 그 텍스트만 옵티컬 들여쓰기(x+1~2) | metadata에서 텍스트 노드 x 확인 후 padding-left |
| 좁은 기기에서만 줄바꿈 | 텍스트가 컨테이너 패딩에 갇힘 (Figma는 최소 마진까지 확장 허용) | `width: calc(100% + Npx)` 또는 `min(고정폭, calc(100% + Npx))`; 그래도 1~2px 모자라면 해당 폭 이하 미디어쿼리로 자간 -0.1px |

### 4. 수정 → 재검증 루프

수정 → 재캡쳐 → 재측정 → **겹침 육안** → 다르면 반복. 겹침 확인은 두 가지 뷰:

- **색 유지 겹침**(Figma 50% 투명도 위에): 최종 판정용. 8배 확대로 본다.
- **색 제거 위치 겹침**(파랑=우리/빨강=Figma/보라=일치): 색 차이·렌더링 차이에 속지 않고 순수 위치만 볼 때.

### 4.5 확대 배율과 문구 다른 캡쳐

- 겹침 판독은 **16배**(픽셀 단위 판독), 넓은 영역 훑기는 8배. 확대 크롭이 화면에서 축소돼 보이면 배율을 의심하라 — "너무 작게 확대했나"는 실제 지적이다. 크롭 폭 × 배율이 뷰어 폭을 넘으면 영역을 쪼개서 본다.
- **문구가 다른 두 캡쳐**(목데이터 vs Figma 예시 문구)는 겹침 육안이 오판하기 쉽다 — 문구가 동일한 앵커(숫자 "01."/날짜, 아이콘, 디바이더, pill 경계)만 육안 신뢰하고, 나머지는 잉크 런/DOM 수치로 판정한다. 리포트에도 "문구는 목데이터 — 위치·간격만 볼 것"을 명시한다.

### 5. 보고

리포트(HTML artifact): 영역별로 [① 우리 화면 | ② Figma | 겹침(투명도 슬라이더)] 3열, 사진 영역 회색 마스킹, 라벨은 색 태그로 명확히("① 우리 화면(지금 코드)" / "② 피그마 원본"). 티켓별 수정 내역 표 + 테스트 결과.

- **섹션을 추가하면 하단 요약(현황) 리스트에도 같은 항목을 추가한다** — 사용자는 요약만 보고 "빠졌다"고 인지한다(실제 발생).
- 슬라이더 동작 검증은 **두 이미지의 픽셀이 실제로 다른 좌표**에서 알파 0/100 픽셀 변화를 확인한다(동일 픽셀 지점에서 재면 false negative).
- 같은 파일 경로로 재발행하면 artifact URL이 유지된다 — 캡쳐를 갱신했으면 반드시 재빌드+재발행.

## 안티패턴 (이 세션에서 실제로 저지른 실수들)

- ❌ 수치만 보고 "일치"라고 보고 (→ 육안 겹침이 판정 기준)
- ❌ 리포트 뷰포트(1180px)에서 캡쳐해 900px 이미지로 겹침 (→ 크기 불일치)
- ❌ 가로만 측정하고 세로 오프셋을 놓침
- ❌ 색 판정식이 다른 두 요소를 비교해 가짜 1px을 "보정" → 멀쩡한 밑선 파괴
- ❌ 눈대중으로 gap을 늘렸다 줄였다 (→ 반드시 잉크 실측 후 계산된 값만 적용)
- ❌ 0.5px position 이동 시도 (→ 크롬 스냅으로 무효, 실측으로 확인됨)
- ❌ 경계 AA 픽셀(±1 flutter)을 맞추려 반복 수정
- ❌ Figma 구버전 색을 "정답"으로 적용 (→ 색은 사용자/디자이너 확인 필수)
- ❌ 아이콘/텍스트 측정 x범위를 미리 갈라서 잼 → 간격 1px 차이를 "일치"로 오판 (→ 철칙 11: 무가정 전체 런)
- ❌ Figma 명목 gap을 그대로 적용하고 종료 (→ DS 아이콘 잉크 폭 차이로 시각 간격 어긋남 — 철칙 12)
- ❌ 간격을 맞추며 어긋남을 다른 곳(아이콘 좌측 여백)으로 밀어놓고 보고 (→ 사용자가 "그룹이 밀렸다"로 재지적 — 트레이드오프는 계산해서 가장 안 보이는 곳이 아니라 **잉크 대조가 최대로 일치하는 배치**를 고르고, 남는 px은 보고에 명시)
- ❌ 지적받은 요소만 고치고 종료 → 같은 컴포넌트에서 다음 지적 반복 (→ 철칙 13: 전수 대조)
- ❌ 요약(현황) 리스트 갱신 누락 → 사용자가 "리포트에 빠졌다"고 인지
